<h1>Pages#index</h1>
<p>Find me in app/views/pages/index.html.erb</p>
<% if current_user&.subscribed? %>
  SUBSCRIBED - <%= current_user.stripe_subscription_id %>
<% else %>
  <p><%= link_to "Subscribe Now!", new_subscription_path, class: 'btn btn-primary' %></p>
<% end %>